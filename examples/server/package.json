{
  "name": "mindoodb-example-server",
  "version": "0.0.3",
  "private": true,
  "description": "Example MindooDB sync server with client-to-server and server-to-server sync support",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "ts-node src/server.ts",
    "init": "ts-node src/serverinit.ts",
    "add-to-network": "ts-node src/add-to-network.ts"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^4.18.2",
    "express-rate-limit": "^8.2.1",
    "helmet": "^8.1.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^4.17.21",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.0"
  }
}
